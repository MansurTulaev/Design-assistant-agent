## MCP Figma Kontur UI Scanner
MCP-сервер для автоматической сборки React-компонентов на основе макетов Figma и библиотеки Kontur UI. Реализует архитектуру "Макет как инструкция по сборке".

### 🎯 Концепция
- Агент получает задачу (например, "Создай форму входа") и использует MCP-сервер для:

- Сканирования библиотеки компонентов Kontur UI из Git-репозитория

- Анализа макетов Figma для понимания структуры интерфейса

- Сопоставления элементов макета с готовыми компонентами

- Генерации React-кода с правильными импортами и пропсами

### ✨ Возможности
- 🔍 Сканирование Git-репозитория retail-ui для получения метаданных компонентов (пропсы, типы, варианты)

- 🎨 Анализ Figma-макетов для извлечения структуры и свойств элементов

- 🔗 Интеллектуальное сопоставление элементов макета с компонентами библиотеки

- 💻 Генерация React-кода с импортами из @skbkontur/react-ui

- 📊 Экспорт данных в JSON для использования AI-агентом

- 🐳 Готовый Docker-образ для быстрого развертывания

### 🚀 Быстрый старт
Docker (рекомендуется)
```bash
# Сборка образа
docker build -t mcp-figma-kontur .

# Запуск контейнера
docker run -p 8000:8000 \
  -e FIGMA_ACCESS_TOKEN=ваш_токен_figma \
  -e KONTUR_UI_FILE_ID=KQc2jUV5CuCDqZ7hHTX0vc \
  -e TEST_FILE_ID=d4qp6XOTZc3abUbq5UUDe7 \
  mcp-figma-kontur
  ```
### Локальный запуск
```bash
# Установка зависимостей
uv sync

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env, добавив ваш токен Figma

# Запуск сервера
uv run python src/server.py
```
### ⚙️ Конфигурация
Создайте файл .env со следующими переменными:

``env
# Обязательные
FIGMA_ACCESS_TOKEN=ваш_личный_токен_figma
KONTUR_UI_FILE_ID=KQc2jUV5CuCDqZ7hHTX0vc  # ID файла библиотеки Kontur UI
TEST_FILE_ID=d4qp6XOTZc3abUbq5UUDe7       # ID тестового файла Figma

# Опциональные
PORT=8000                                 # Порт сервера
LOG_LEVEL=INFO                            # Уровень логирования
MAX_GIT_FILES=50                          # Макс. файлов для сканирования в Git
``

### 🔧 Доступные инструменты
1. scan_git_components
Сканирует Git-репозиторий retail-ui и извлекает информацию о компонентах.

Возвращает:

- Список компонентов с их пропсами и типами

- Пути импорта для React

- Документацию из TypeScript-комментариев

2. get_design_system_components
- Получает компоненты из Figma-библиотеки Kontur UI через Figma API.

Возвращает:

- Структуру компонентов и их варианты

- Базовые стили (цвета, шрифты)

Метаданные библиотеки

3. analyze_figma_layout
- Анализирует любой макет Figma для понимания структуры интерфейса.

Возвращает:

- Иерархию элементов (frames, groups, instances)

- Свойства элементов (позиция, размер, стили)

- Текстовые элементы и их свойства

4. map_layout_to_components
Сопоставляет элементы макета с компонентами библиотеки и генерирует React-код.

Параметры:

layout_file_key: ID файла Figma с макетом

design_system_file_key: ID файла с библиотекой компонентов

generate_code: Генерировать ли React-код (по умолчанию True)

Возвращает:

- Сопоставления элементов с компонентами

- Сгенерированный React-код с импортами

- Уверенность сопоставления для каждого элемента

## 🎯 Пример workflow
Агент получает задачу: "Создай форму входа"

Сканирование библиотеки:

```python
git_data = await scan_git_components()
# Узнает, что Input принимает пропсы: type, label, value
Анализ макета:
``` 

```python
layout = await analyze_figma_layout(file_key="test_file_id")
# Видит: текстовое поле "Email", кнопка "Sign In"
Сопоставление и генерация:
```

```python
result = await map_layout_to_components(
    layout_file_key="test_file_id",
    design_system_file_key="kontur_ui_file_id"
)
```
### Результат - React-компонент:
```jsx
import { Input, Button, Card } from '@skbkontur/react-ui';

export const LoginForm = () => (
  <Card>
    <Input type="email" label="Email" />
    <Button variant="primary">Sign In</Button>
  </Card>
);
```
### 🔌 Интеграция с AI-агентами
Claude Desktop
Добавьте в claude_desktop_config.json:

```json
{
  "mcpServers": {
    "figma-kontur": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "mcp-figma-kontur"],
      "env": {
        "FIGMA_ACCESS_TOKEN": "ваш_токен",
        "KONTUR_UI_FILE_ID": "KQc2jUV5CuCDqZ7hHTX0vc",
        "TEST_FILE_ID": "d4qp6XOTZc3abUbq5UUDe7"
      }
    }
  }
}
```
## Тестирование через MCP Inspector
```bash
# Установка Inspector
npm install -g @modelcontextprotocol/inspector

# Запуск Inspector
mcp-inspector docker exec -i [container_id] uv run python -m mcp client
```
## 🐳 Docker
Сборка и запуск
```bash
# Сборка образа
docker build -t mcp-figma-kontur .

# Запуск контейнера
docker run --rm -p 8000:8000 \
  -e FIGMA_ACCESS_TOKEN=ваш_токен \
  -e KONTUR_UI_FILE_ID=KQc2jUV5CuCDqZ7hHTX0vc \
  -e TEST_FILE_ID=d4qp6XOTZc3abUbq5UUDe7 \
  mcp-figma-kontur
Проверка работы
powershell
# Проверка, что контейнер запущен
docker ps

# Просмотр логов
docker logs -f [container_id]

# Проверка доступности
docker exec [container_id] python -c "print('✅ MCP сервер работает')"
```
## 📁 Структура проекта
```text
mcp-figma-kontur/
├── src/
│   ├── server.py                    # Основной файл сервера
│   ├── mcp_instance.py              # Единый экземпляр FastMCP
│   ├── tools/
│   │   ├── __init__.py              # Экспорт всех инструментов
│   │   ├── scan_git.py              # Сканирование Git-репозитория
│   │   ├── get_design_system.py     # Работа с Figma API
│   │   ├── analyze_layout.py        # Анализ макетов Figma
│   │   └── map_components.py        # Сопоставление и генерация кода
│   ├── metrics.py                   # Prometheus-метрики
│   └── config.py                    # Конфигурация приложения
├── Dockerfile                       # Конфигурация Docker
├── pyproject.toml                   # Зависимости Python
├── .env.example                     # Пример переменных окружения
└── README.md                        # Эта документация
```
## 🛠️ Технологии
- Python 3.12+ - основной язык реализации
- FastMCP 2.13.3 - фреймворк для MCP-серверов
- Figma REST API - получение данных из Figma
- GitPython - работа с Git-репозиториями
- Docker - контейнеризация приложения
- Prometheus - метрики и мониторинг

## 📊 Особенности реализации
### Без Tokens Studio
- Сервер работает без Tokens Studio, используя только стандартное Figma API:

- Получает компоненты, их варианты и базовые стили

- Извлекает структуру макетов и свойства элементов

- Экспортирует данные в JSON для использования агентом

### Архитектура "Макет как инструкция"
- Источник компонентов → Git-репозиторий retail-ui

- Источник дизайна → Figma-макеты

- Логика сопоставления → AI-агент на основе метаданных

- Результат → React-код с готовыми компонентами

## 🤝 Участие в хакатоне
Проект разработан в рамках хакатона по созданию мультиагентной системы для дизайнеров. Основная цель - показать, как AI-агенты могут автоматически создавать UI-компоненты на основе дизайн-макетов и готовых библиотек.

## 🔮 Планы по развитию
- Добавление поддержки других библиотек компонентов

- Улучшение точности сопоставления элементов

- Поддержка генерации кода для Vue.js, Angular

- Кэширование результатов сканирования Git

- Интеграция с Tokens Studio для расширенных возможностей

## 📝 Лицензия
Проект создан для хакатона. Используйте, модифицируйте и делитесь свободно!