# 🎨 Figma to React Agent

Агент для автоматической генерации React/TSX компонентов на основе дизайн-системы и Figma макетов. Решение позволяет создавать production-ready код, используя готовые компоненты из вашей дизайн-системы.

## ✨ Особенности

- **Автоматическая генерация** React/TSX компонентов из описания на естественном языке
- **Интеграция с Figma** через MCP-сервер для получения структуры макетов
- **Использование дизайн-системы** через MCP-сервер для работы с готовыми компонентами
- **Генерация чистого кода** с правильными импортами, TypeScript интерфейсами и документацией
- **Поддержка стриминга** для отображения прогресса генерации
- **Готовность к развертыванию** в платформе AI Agents (cloud.ru)

## 🏗️ Архитектура

```
┌─────────────────┐    ┌──────────────────┐    ┌──────────────────────┐
│   Пользователь   │───▶│  Figma-React     │───▶│   MCP-сервер Figma   │
│     (запрос)     │    │     Agent        │    │   (структура)        │
└─────────────────┘    └──────────────────┘    └──────────────────────┘
                                │
                                ▼
                      ┌──────────────────────┐
                      │   MCP-сервер         │
                      │   Дизайн-системы     │
                      │   (компоненты)       │
                      └──────────────────────┘
                                │
                                ▼
                      ┌──────────────────────┐
                      │   LLM (Qwen)         │
                      │   (генерация кода)   │
                      └──────────────────────┘
                                │
                                ▼
                      ┌──────────────────────┐
                      │   React/TSX компонент │
                      │   (результат)        │
                      └──────────────────────┘
```

## 📋 Предварительные требования

- **Python 3.12+**
- **Docker** (для контейнеризации)
- **Аккаунт в cloud.ru** с доступом к AI Agents
- **Figma API токен** (для доступа к макетам)
- **Доступ к LLM** (Qwen через cloud.ru или другая модель)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd figma-react-agent
```

### 2. Настройка окружения

Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл, указав ваши значения:

```env
# Основные настройки агента
AGENT_NAME=Figma to React Agent
AGENT_DESCRIPTION=Generates React components from Figma designs using your design system
AGENT_VERSION=1.0.0

# LLM Configuration (используйте вашу модель)
LLM_MODEL=hosted_vllm/Qwen/Qwen3-Coder-480B-A35B-Instruct
LLM_API_BASE=https://foundation-models.api.cloud.ru/v1
LLM_API_KEY=ваш-api-ключ

# MCP серверы (через запятую БЕЗ пробелов)
MCP_URL=http://figma-mcp:8000/sse,http://designsystem-mcp:8000/sse

# Конфигурация сервера
PORT=10000
URL_AGENT=https://ваш-agent-id-agent.ai-agent.inference.cloud.ru

# Сервисный аккаунт Evolution
EVOLUTION_SERVICE_ACCOUNT_KEY_ID=ваш-key-id
EVOLUTION_SERVICE_ACCOUNT_KEY_SECRET=ваш-key-secret

# Логирование
LOG_LEVEL=INFO
ENABLE_PHOENIX=false
```

### 3. Установка зависимостей

```bash
# Установка Python зависимостей
pip install -e .

# Или через poetry (если используется)
poetry install
```

### 4. Запуск локально

```bash
# Запуск агента локально для тестирования
python -m src.start_a2a
```

Агент будет доступен по адресу: `http://localhost:10000`

## 🐳 Запуск через Docker

### Сборка образа

```bash
docker buildx build --platform linux/amd64 -t figma-react-agent .
```

### Тегирование и загрузка в Artifact Registry

```bash
# Тегирование
docker tag figma-react-agent:latest cloudru-labs.cr.cloud.ru/figma-react-agent:v1.0.0

# Загрузка (предварительно выполните аутентификацию)
docker push cloudru-labs.cr.cloud.ru/figma-react-agent:v1.0.0
```

### Запуск контейнера локально

```bash
docker run -p 10000:10000 --env-file .env figma-react-agent
```

## ☁️ Развертывание в AI Agents (cloud.ru)

### 1. Создание реестра в Artifact Registry

1. Откройте UI cloud.ru
2. Перейдите в **Artifact Registry**
3. Создайте новый реестр для Docker образов

### 2. Создание репозитория

В созданном реестре создайте репозиторий для вашего агента.

### 3. Загрузка Docker образа

Выполните команды из раздела "Запуск через Docker" для загрузки образа в ваш репозиторий.

### 4. Создание агента в UI cloud.ru

1. Перейдите в раздел **AI Agents**
2. Нажмите **Создать агента**
3. Заполните параметры:
   - **Название агента**: Figma to React Agent
   - **Docker образ**: выберите загруженный образ
   - **Системный промпт**: укажите путь к файлу или вставьте текст
   - **MCP серверы**: добавьте URL ваших MCP серверов
4. Нажмите **Создать**

### 5. Использование агента

После создания вы можете:
- Общаться с агентом через UI чата
- Интегрировать через API
- Использовать в рабочих процессах

## 📖 Примеры использования

### Пример 1: Генерация карточки товара

**Запрос:**
```
Создай карточку товара для интернет-магазина. 
Должна включать: изображение, название, описание, цену и кнопку "В корзину".
```

**Ответ агента:**
```tsx
import { Card, Image, Text, Button, Stack } from '@company-ui/core';

interface ProductCardProps {
  title: string;
  description: string;
  price: number;
  imageUrl: string;
  onAddToCart: () => void;
}

export const ProductCard = ({ 
  title, 
  description, 
  price, 
  imageUrl, 
  onAddToCart 
}: ProductCardProps) => (
  <Card variant="elevated" padding="lg" width="300px">
    <Stack spacing="md">
      <Image 
        src={imageUrl} 
        alt={title} 
        width={280} 
        height={200}
        objectFit="cover"
      />
      <Stack spacing="sm">
        <Text variant="heading" size="lg">{title}</Text>
        <Text color="secondary" lineClamp={2}>{description}</Text>
        <Stack direction="horizontal" justify="space-between" align="center">
          <Text variant="price" size="xl">${price.toFixed(2)}</Text>
          <Button 
            variant="primary" 
            size="medium"
            onClick={onAddToCart}
          >
            В корзину
          </Button>
        </Stack>
      </Stack>
    </Stack>
  </Card>
);
```

### Пример 2: Генерация формы входа

**Запрос:**
```
Создай форму входа с полями email и пароль, 
чекбоксом "Запомнить меня" и кнопкой "Войти".
```

**Ответ агента:**
```tsx
import { Card, Input, Button, Checkbox, Stack, Form } from '@company-ui/core';
import { useState } from 'react';

interface LoginFormProps {
  onSubmit: (email: string, password: string, rememberMe: boolean) => void;
}

export const LoginForm = ({ onSubmit }: LoginFormProps) => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [rememberMe, setRememberMe] = useState(false);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    onSubmit(email, password, rememberMe);
  };

  return (
    <Card variant="outlined" padding="xl" maxWidth="400px">
      <Form onSubmit={handleSubmit}>
        <Stack spacing="lg">
          <Input
            label="Email"
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="your@email.com"
            required
            autoComplete="email"
          />
          
          <Input
            label="Пароль"
            type="password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="••••••••"
            required
            autoComplete="current-password"
          />
          
          <Checkbox
            checked={rememberMe}
            onChange={(e) => setRememberMe(e.target.checked)}
            label="Запомнить меня"
          />
          
          <Button 
            type="submit" 
            variant="primary" 
            size="large"
            fullWidth
          >
            Войти
          </Button>
        </Stack>
      </Form>
    </Card>
  );
};
```

## ⚙️ Конфигурация MCP серверов

Агент работает с двумя MCP серверами:

### 1. MCP сервер для Figma

**Назначение:** Получение структуры компонентов из Figma

**Пример конфигурации:**
```python
# mcp-figma/server.py
@tool
async def get_component_structure(component_name: str):
    """Получить JSON-структуру компонента из Figma"""
    # Использует Figma API
    return {
        "name": component_name,
        "layers": [...],
        "styles": {...}
    }
```

### 2. MCP сервер для дизайн-системы

**Назначение:** Получение метаданных компонентов из вашей дизайн-системы

**Пример конфигурации:**
```python
# mcp-designsystem/server.py
@tool
async def get_component_metadata(component_name: str):
    """Получить метаданные компонента из дизайн-системы"""
    # Анализирует вашу библиотеку компонентов
    return {
        "name": "Button",
        "importPath": "@company-ui/button",
        "props": {
            "variant": ["primary", "secondary", "ghost"],
            "size": ["sm", "md", "lg"]
        }
    }
```

## 🔧 Переменные окружения

| Переменная | Обязательная | Значение по умолчанию | Описание |
|------------|--------------|----------------------|-----------|
| `AGENT_NAME` | Да | - | Название агента (отображается в UI) |
| `AGENT_DESCRIPTION` | Да | - | Описание агента (отображается в UI) |
| `AGENT_VERSION` | Нет | `1.0.0` | Версия агента |
| `AGENT_SYSTEM_PROMPT_FILE` | Нет | `./agent-config/system-prompt.txt` | Путь к файлу с системным промптом |
| `LLM_MODEL` | Да | - | Модель LLM (например, `hosted_vllm/Qwen/Qwen3-Coder-480B-A35B-Instruct`) |
| `LLM_API_BASE` | Да | - | Базовый URL API LLM |
| `LLM_API_KEY` | Да | - | API ключ для доступа к LLM |
| `MCP_URL` | Да | - | Список URL MCP серверов через запятую |
| `PORT` | Нет | `10000` | Порт для запуска агента |
| `URL_AGENT` | Нет | - | URL агента в платформе |
| `EVOLUTION_SERVICE_ACCOUNT_KEY_ID` | Да | - | ID ключа сервисного аккаунта |
| `EVOLUTION_SERVICE_ACCOUNT_KEY_SECRET` | Да | - | Секрет ключа сервисного аккаунта |
| `ENABLE_PHOENIX` | Нет | `false` | Включение трассировки через Phoenix |
| `PHOENIX_ENDPOINT` | Нет | - | Endpoint для отправки трасс |
| `LOG_LEVEL` | Нет | `INFO` | Уровень логирования |

## 🧪 Тестирование

Проект включает полный набор тестов:

```bash
# Установка dev-зависимостей
pip install -r requirements-dev.txt

# Запуск всех тестов
pytest tests/ -v

# Запуск с покрытием кода
pytest tests/ --cov=src --cov-report=html

# Запуск только unit-тестов
pytest tests/ -m unit -v

# Запуск интеграционных тестов
pytest tests/ -m integration -v
```

## 🐛 Отладка и логирование

### Включение детального логирования

Установите `LOG_LEVEL=DEBUG` в `.env` файле:

```env
LOG_LEVEL=DEBUG
```

### Просмотр логов в Docker

```bash
docker logs <container_id> -f
```

### Использование Phoenix для трассировки

1. Установите `ENABLE_PHOENIX=true` в `.env`
2. Укажите `PHOENIX_ENDPOINT`
3. Перезапустите агент

## 🔄 Обновление агента

### 1. Внесение изменений в код

```bash
# Внесение изменений в src/
git add src/
git commit -m "Описание изменений"
```

### 2. Обновление версии

Обновите `AGENT_VERSION` в `.env` файле и `pyproject.toml`.

### 3. Пересборка Docker образа

```bash
docker buildx build --platform linux/amd64 -t figma-react-agent:v1.1.0 .
docker tag figma-react-agent:v1.1.0 cloudru-labs.cr.cloud.ru/figma-react-agent:v1.1.0
docker push cloudru-labs.cr.cloud.ru/figma-react-agent:v1.1.0
```

### 4. Обновление агента в cloud.ru

1. Откройте UI cloud.ru
2. Перейдите к вашему агенту
3. Обновите Docker образ на новую версию
4. Сохраните изменения

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add some amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи агента: `docker logs <container_id>`
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте подключение к MCP серверам
4. Убедитесь, что API ключи действительны

Для дополнительной помощи создайте issue в репозитории проекта.

---

## 🎯 Краткая памятка по командам

```bash
# Локальная разработка
python -m src.start_a2a

# Сборка Docker образа
docker buildx build --platform linux/amd64 -t figma-react-agent .

# Запуск тестов
pytest tests/ -v

# Загрузка в Artifact Registry
docker tag figma-react-agent:latest cloudru-labs.cr.cloud.ru/figma-react-agent:v1.0.0
docker push cloudru-labs.cr.cloud.ru/figma-react-agent:v1.0.0
```

**Примечание:** Этот README предполагает, что у вас уже настроены MCP серверы для Figma и дизайн-системы. Для полной функциональности необходимо их развернуть и настроить.